syntax = "proto3";

package raftcache;

message Node {
  enum Statuses {
    ALONE = 0;
    INGROUP = 1;
    DISCONNECTED = 2;
  }

  string Group = 1;
  string AdminAddr = 2;
  string ListenAddr = 3;
  Statuses Status = 4;
}

message JoinResp {
  enum Results {
    SUCCESS = 0;
    ALREADYJOINED = 1;
    REJECTED = 2;
  }

  Results Result = 1;
  repeated Node GroupNodes = 2;
}

message LeaveResp {}

service RaftCache {
  rpc Join(Node) returns (JoinResp) {};
  rpc Leave(Node) returns (LeaveResp) {};
}